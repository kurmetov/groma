# Fixtures

Only small fixtures with explicit redistribution permission belong here. Real
`.rvt`/`.rfa` files are ignored by default. Record provenance, license, Revit
release, and SHA-256 for every committed sample.

The current test suite creates synthetic CFB files at runtime so repository
tests do not depend on proprietary or ambiguously licensed model content.

