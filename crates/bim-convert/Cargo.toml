[package]
name = "bim-convert"
description = "Source-format identification and the conversion pipeline shared by every reader"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
bim-core = { path = "../bim-core" }

[lints]
workspace = true
