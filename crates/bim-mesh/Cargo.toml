[package]
name = "bim-mesh"
description = "Tessellation of canonical BIM geometry into indexed triangle meshes"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
bim-core = { path = "../bim-core" }

[lints]
workspace = true
